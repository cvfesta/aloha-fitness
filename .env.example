# Copy this file to .env (or .env.local) and fill in real values.
# Vite only exposes variables prefixed with VITE_ to the client bundle.

# Mixpanel project token. If unset, analytics calls are no-ops.
VITE_MIXPANEL_TOKEN=
